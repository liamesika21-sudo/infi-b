# סיכום המערכת — אינפי ב׳ (Calculus 2) הכנה למבחן

> מסמך הסבר: מה המערכת עושה כשמעלים אליה חומרים (הרצאות / תרגולים / סיכומים / מבחני עבר), איך היא בנויה, ואילו דפים קיימים ומה כל דף נותן לקראת המבחן.

**מטרה:** מערכת הכנה ממוקדת לאינפי ב׳, מועד א׳, יעד ציון 90+. עברית-first, עם רינדור מתמטי (KaTeX).
**מועד מבחן:** 01.07.2026.

---

## 1. מה קורה כשאני מעלה חומר?

החומרים מועלים לתיקיית `docs/` לפי סוג, וכל סוג נכנס לתת-תיקייה משלו:

| תיקייה | מה שמים שם | איכות הנתונים |
|--------|------------|----------------|
| `docs/lecture/` | הרצאות 1–10 (PDF) | כתב יד → OCR גרוע. **לא** מקור תוכן אוטומטי. חולץ ידנית ל-`lecture-knowledge.json` |
| `docs/recitation/` | תרגולים 1–10 (עברית מודפסת) | **טובה** — מתמטיקה אמיתית בעברית+LaTeX |
| `docs/summerize/` | סיכומים דיגיטליים | **טובה** — מקור עיקרי לתוכן ההרצאות |
| `docs/hw/` | פתרונות מטלות (Exercise 1–8) | **טובה** — שאלות בסגנון מבחן |
| `docs/past-exams/` | מבחני מועד א׳/ב׳ 2022–2025 + סימולציה | **טובה** — מקור לשאלות אמת |
| `docs/past-exams-hebrew/` | מבחני עבר בעברית | טובה |
| `docs/guide/` | מסמכי הכוונה/סילבוס | רקע |

### זרימת העיבוד (Pipeline)

מעלים קובץ → מריצים סקריפט → נוצר JSON שהאתר קורא ומציג. שלושה שלבים:

1. **`npm run process:calculus2`** — סורק את `docs/`, מסווג כל קובץ (לפי תיקייה+שם), ומחלץ טקסט מה-PDF.
   נוצר: `source-files.json`, `extracted-text-index.json`, `topic-map.json`, `week-map.json`, `processing-summary.json`.
   קוד: [scripts/process-calculus2.ts](scripts/process-calculus2.ts), [src/lib/calculus2/content-parser.ts](src/lib/calculus2/content-parser.ts).

2. **`npm run analyze:calculus2`** — מריץ ניתוח על הטקסט שחולץ (מזהה הגדרות/משפטים/הוכחות/דוגמאות/טעויות נפוצות לפי מילות מפתח עברית+אנגלית), ובסוף מייצר את הנתונים המורחבים.
   נוצר: `lecture-analysis.json`, `recitation-analysis.json`, `homework-analysis.json`, `summary-analysis.json`, `past-exam-analysis.json`, ובנקים: `formula-bank.json`, `theorem-bank.json`, `proof-pattern-bank.json`, `question-bank.json`, וכן `exam-priority-map.json`, `exam-plan.json`, `mentor-knowledge-base.json`.
   קוד: [scripts/analyze-calculus2.ts](scripts/analyze-calculus2.ts).

3. **`npm run generate:simulations`** — מייצר/מרענן את מבחני הסימולציה והסיכומים בלבד (`simulation-exams.json`, `recitation-summaries.json`, `lecture-summaries.json`, `homework-priority-map.json`).
   קוד: [scripts/generate-extended-data.ts](scripts/generate-extended-data.ts).

> **חשוב על איכות:** הרצאות בכתב יד עברו OCR גרוע, ולכן התוכן ה"אמיתי" של ההרצאות נמצא ב-`lecture-knowledge.json` — חילוץ ידני מילה-במילה של 10 ההרצאות (37 הגדרות + 55 משפטים, ממוספרים בדיוק כמו בהרצאה, המספור מתחיל מחדש בכל הרצאה). זהו האינדקס הסמכותי. הרצאות 1–2 מסומנות `verified:true` (כולל הוכחות מלאות); 3–10 עדיין ניסוחים בלבד.

### מיפוי הרצאה → שבוע באתר
L1→שבוע2, L2/L3→שבוע4, L4→שבוע5, L5→שבוע6, L6→שבוע7, L7→שבוע8, L8/L9/L10→שבוע9.
שבועות 1 ו-3 (לופיטל/דרבו/MVT) אין להם מקור הרצאה — החומר מגיע מהתרגולים.

---

## 2. איך המערכת בנויה (ארכיטקטורה)

- **Framework:** Next.js 16.2.6 (Turbopack), React 19, Tailwind v4, KaTeX לרינדור מתמטי.
- **`src/app/`** — כל הדפים (routes) + ה-API.
- **`src/lib/calculus2/`** — לב הלוגיקה: טיפוסים, סיווג קבצים, מיפוי שבועות, pipeline, scoring, prompts של המנטור, וקריאת הנתונים המחוללים.
- **`src/components/study/`** — רכיבי תצוגה לשימוש חוזר (StudyCard, MathContent, TheoremBlock, FormulaBlock, QuestionBlock, SimulationCard, WeekCard, Badges).
- **`data/generated/calculus2/`** — כל ה-JSON המחולל שהדפים קוראים ממנו.
- **`data/auth-users.json`** — שליטת גישה (משתמשי Pro למנטור).

**עיקרון מנחה:** הרצאה N = חומר חדש לשבוע N; תרגול N = בדרך כלל מתרגל את הרצאה N-1; מטלה N = לפי רצף השבוע. המערכת לא מאחדת אותם לנושא אחד בלי ניתוח.

---

## 3. הדפים — מה כולל כל דף

### דפי ליבה (בתפריט הראשי)

| דף | נתיב | מה הוא נותן לקראת המבחן |
|----|------|------------------------|
| **דשבורד** | `/dashboard` | מצב החומרים, רמת מוכנות, והפעולה המומלצת הבאה |
| **מפת שבועות** | `/weeks` ו-`/weeks/[n]` | 13 שבועות עם היסט הרצאה–תרגול; דף שבוע בודד מציג את כל ההגדרות/משפטים/הוכחות של אותו שבוע |
| **נושאים** | `/topics` | מבנה topic-first: כל נושא, תתי-נושאים וקישור לחומר |
| **נוסחאות** | `/formulas` | בנק נוסחאות עם תנאי שימוש, מתי להשתמש וטעויות נפוצות |
| **משפטים** | `/theorems` | ניסוח, הנחות, מסקנה והוכחה — מתוך האינדקס הסמכותי |
| **תבניות הוכחה** | `/proof-patterns` | דפוסי הוכחה שחוזרים במבחנים |
| **תרגול** | `/practice` | שאלות לפי נושא, רמת קושי וחשיבות למבחן |
| **מבחני עבר** | `/past-exams` | תדירות נושאים, דפוסים חוזרים וסדר פתרון מומלץ |
| **חזרת מטלות** | `/homework-review` | שאלות מטלה שקרובות בסגנון לשאלות המבחן |
| **חזרה מהירה** | `/quick-review` | רגע לפני המבחן: נוסחאות ומשפטים בתמצית |
| **מעקב שליטה** | `/progress` | סטטוס שליטה לכל נושא + חזרה מרווחת (spaced repetition) |
| **מנטור** | `/mentor` | צ׳אט AI מודע-קורס (ראה סעיף 4) |
| **הערות מקס** | `/instructor-notes` | תובנות מהתמלולים, דוגמאות נגדיות, מה קריטי למבחן |

### דפים נוספים

| דף | נתיב | תיאור |
|----|------|--------|
| **תוכנית קרב** | `/battle-plan` | תוכנית למידה ממוקדת מבחן עם מעקב התקדמות |
| **הגדרות** | `/definitions` | כל ההגדרות הפורמליות מהאינדקס הסמכותי |
| **מפת אינטואיציה** | `/intuition-map` | הסברים אינטואיטיביים לנושאים מופשטים |
| **מחברת** | `/notebook` | מחברת אישית לרישום פתרונות/הערות |
| **סימולציות** | `/simulations` ו-`/simulations/[id]` | מבחני סימולציה מלאים משאלות אמת שחולצו |
| **פתרונות מטלות** | `/homework-solutions` | פתרונות מלאים למטלות |
| **מדריך הוכחות** | `/proof-guide` | מדריך שיטתי לכתיבת הוכחות |
| **מדריך תלמיד** | `/student-guide` | הכוונה כללית לשימוש במערכת |
| **תוכנית לימוד** | `/study-plan` | לוח זמנים ללמידה עד המבחן |
| **הכנה לבוחן** | `/quiz-prep` | הכנה ממוקדת לבחנים |
| **תשלום** | `/payment` (+ success/failure) | רכישת Pro |
| **אדמין** | `/admin` | ניהול |
| **Dev** | `/dev/analysis`, `/dev/extraction` | כלי פיתוח לבדיקת החילוץ והניתוח |

---

## 4. מערכת המנטור (Pro בלבד)

צ׳אט AI ב-`/mentor` למשתמשי Pro (תשלום 14 ₪ → ~150 הודעות).

- **גישה:** מיילים ב-`proEmails` בתוך `data/auth-users.json` (או `PRO_EMAILS` בסביבת production).
- **קרדיטים:** מונה לכל משתמש ב-Redis (Upstash), מפתח `mentor:usage:{email}`, מגבלה 150 הודעות, מחויב לפני הקריאה.
- **מודל:** `claude-haiku-4-5-20251001` (נבחר לעלות נמוכה).
- **System prompt:** נבנה מ-`max-insights.json` + `lecture-knowledge.json` (האינדקס הסמכותי) + הגדרות פורמליות. המנטור מצוטט מהאינדקס בדיוק כדי למנוע "המצאה" של משפטים ממוספרים.
- **תחום:** עונה רק על שאלות אינפי ב׳ (גבולות, סדרות, אינטגרלים, טורים, טורי חזקות, טיילור).
- **API:** `GET /api/mentor/status` (מצב מנוי+ניצול), `POST /api/mentor/chat` (תשובה בסטרימינג).

---

## 5. פקודות מהירות

```bash
npm run dev                  # שרת פיתוח
npm run process:calculus2    # שלב 1: סריקה + חילוץ טקסט מ-PDF
npm run analyze:calculus2    # שלב 2: ניתוח + יצירת כל הבנקים והתוכניות
npm run generate:simulations # שלב 3: רענון סימולציות וסיכומים בלבד
npm run build                # build לפרודקשן
```

**זרימה טיפוסית להוספת חומר:** מעלים PDF ל-`docs/<סוג>/` → `process:calculus2` → `analyze:calculus2` → רענון בדפדפן.
